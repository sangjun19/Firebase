package com.example.goodhouse;

import java.util.HashMap;

public class Appart {
    int address, room;

    //값을 추가할때 쓰는 함수, MainActivity에서 addanimal함수에서 사용할 것임.
    Appart (int address, int room) {
        this.address = address;
        this.room = room;
    }
}